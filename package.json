{
  "name": "yjs-lab",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter frontend run dev\" \"pnpm --filter backend run start:dev\"",
    "build": "concurrently \"pnpm --filter frontend run build\" \"pnpm --filter backend run build\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
